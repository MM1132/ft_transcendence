meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{API_BASE_URL}}/session
  body: json
  auth: none
}

body:json {
  {
    "username": "RobertReimann",
    "password": "verysecretpass"
  }
  
}

script:post-response {
  let token = res.body.sessionToken;
  
  bru.setVar("token", token);
  console.log(`token set: ${token}`);
  
}

meta {
  name: Get User Settings
  type: http
  seq: 4
}

get {
  url: {{API_BASE_URL}}/user/me/settings
  body: none
  auth: inherit
}

headers {
  x-session-token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
